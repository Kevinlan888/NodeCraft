<UserControl x:Class="NodeCraft.BuiltIn.Views.ToStringView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="150">
    <Border Padding="8"
            CornerRadius="8"
            Background="{DynamicResource colorSubtleBackground}"
            BorderBrush="{DynamicResource colorNeutralStroke1}"
            BorderThickness="1">
        <StackPanel>
            <TextBlock Text="To String"
                       FontSize="18"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource colorNeutralForeground1}"
                       Margin="0,4,0,6" />
            <TextBlock x:Name="Input" Text="Convert any value to text"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Foreground="{DynamicResource colorNeutralForeground3}"
                       Margin="0,0,0,8" />
        </StackPanel>
    </Border>
</UserControl>
