<UserControl x:Class="NodeCraft.BuiltIn.Views.TextPreviewView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="200">
    <Border Padding="8"
            CornerRadius="8"
            Background="{DynamicResource colorSubtleBackground}"
            BorderBrush="{DynamicResource colorNeutralStroke1}"
            BorderThickness="1">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Preview"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource colorNeutralForeground1}"
                       Margin="0,0,0,4" />
                    <TextBlock Text="显示任意输入的文本结果"
                       Foreground="{DynamicResource colorNeutralForeground3}"
                       Margin="0,0,0,6" />
                </StackPanel>
                <TextBox x:Name="PreviewText"
                   Text="等待执行后显示文本结果"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource colorNeutralForeground2}"
                   IsReadOnly="True" VerticalContentAlignment="Stretch"/>
            </DockPanel>
        </ScrollViewer>
    </Border>
</UserControl>
