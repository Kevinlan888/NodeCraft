<UserControl x:Class="NodeCraft.BuiltIn.Views.MergeFlowView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="150">
    <Border Padding="8" CornerRadius="8" Background="{DynamicResource colorSubtleBackground}" BorderBrush="{DynamicResource colorNeutralStroke1}" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="Merge Flow" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{DynamicResource colorNeutralForeground1}" Margin="0,4,0,6" />
            <TextBlock Text="Continue when any branch is active" TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource colorNeutralForeground3}" Margin="0,0,0,8" />
        </StackPanel>
    </Border>
</UserControl>
