<UserControl x:Class="NodeCraft.BuiltIn.Views.AppendTextEditor"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="180">
    <Border Padding="8"
            CornerRadius="8"
            Background="{DynamicResource colorSubtleBackground}"
            BorderBrush="{DynamicResource colorNeutralStroke1}"
            BorderThickness="1">
        <StackPanel>
            <TextBlock Text="Suffix"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource colorNeutralForeground1}"
                       Margin="0,0,0,4" />
            <TextBlock Text="给字符串追加后缀"
                       Foreground="{DynamicResource colorNeutralForeground3}"
                       Margin="0,0,0,6" />
            <TextBox x:Name="SuffixEditor" />
        </StackPanel>
    </Border>
</UserControl>
