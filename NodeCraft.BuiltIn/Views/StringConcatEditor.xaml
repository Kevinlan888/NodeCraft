<UserControl x:Class="NodeCraft.BuiltIn.Views.StringConcatEditor"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="180">
    <Border Padding="8"
            CornerRadius="8"
            Background="{DynamicResource colorSubtleBackground}"
            BorderBrush="{DynamicResource colorNeutralStroke1}"
            BorderThickness="1">
        <StackPanel>
            <TextBlock Text="String Concat"
                       FontSize="18"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource colorNeutralForeground1}"
                       Margin="0,4,0,6" />
            <TextBlock Text="Separator"
                       Foreground="{DynamicResource colorNeutralForeground3}"
                       Margin="0,0,0,3" />
            <TextBox x:Name="SeparatorEditor"
                     MinWidth="120"
                     Padding="5,2"
                     ToolTip="Text inserted between inputs" />
        </StackPanel>
    </Border>
</UserControl>
