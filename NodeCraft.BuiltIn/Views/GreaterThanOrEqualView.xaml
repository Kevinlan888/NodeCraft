<UserControl x:Class="NodeCraft.BuiltIn.Views.GreaterThanOrEqualView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="110">
    <Border Padding="8"
            CornerRadius="8"
            Background="{DynamicResource colorSubtleBackground}"
            BorderBrush="{DynamicResource colorNeutralStroke1}"
            BorderThickness="1">
        <StackPanel>
            <TextBlock Text=">="
                       FontSize="22"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource colorNeutralForeground1}"
                       Margin="0,4,0,6" />
            <TextBlock Text="A is greater than or equal to B"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Foreground="{DynamicResource colorNeutralForeground3}"
                       Margin="0,0,0,8" />
        </StackPanel>
    </Border>
</UserControl>
