<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="180">
    <Border x:Name="EditorCard"
            Padding="8"
            CornerRadius="8"
            Background="{DynamicResource colorSubtleBackground}"
            BorderBrush="{DynamicResource colorNeutralStroke1}"
            BorderThickness="1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource colorSubtleBackground}" />
                <Setter Property="BorderBrush" Value="{DynamicResource colorNeutralStroke1}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsChecked, ElementName=AccentSwitch}" Value="True">
                        <Setter Property="Background" Value="{DynamicResource colorBrandBackground2}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource colorBrandStroke1}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <StackPanel>
            <TextBlock Text="Sample Value"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource colorNeutralForeground1}"
                       Margin="0,0,0,6" />

            <CheckBox x:Name="AccentSwitch"
                      Content="Accent style"
                      Margin="0,0,0,6" />

            <TextBox x:Name="ValueEditor"
                     Text="{Binding ValueText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Border>
</UserControl>
