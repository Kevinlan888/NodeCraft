<UserControl x:Class="NodeCraft.Vision.Views.FlowImagePreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="220"
             MinHeight="180">
    <Border Padding="8"
            CornerRadius="8"
            Background="{DynamicResource colorSubtleBackground}"
            BorderBrush="{DynamicResource colorNeutralStroke1}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Background="{DynamicResource colorNeutralBackground1}">
                <Image x:Name="PreviewImage"
                       Stretch="Uniform" />
            </Border>
            <StackPanel Grid.Row="1"
                        Margin="0,6,0,0">
                <TextBlock x:Name="FrameText"
                           Foreground="{DynamicResource colorNeutralForeground1}" />
                <TextBlock x:Name="StatusText"
                           Foreground="{DynamicResource colorNeutralForeground3}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
